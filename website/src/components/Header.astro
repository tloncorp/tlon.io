---
import { getHeaderNav } from "../utils/api";
const links = await getHeaderNav();
---

<header
  role="banner"
<<<<<<< Updated upstream
  class="flex items-center justify-between leading-none sm:px-8 sm:py-6"
>
  <a class="inline-block w-12 p-4" href="/">
=======
  class="flex items-center justify-between bg-mono-100 p-8 leading-none"
>
  <a class="inline-block w-12 bg-primary p-4" href="/">
>>>>>>> Stashed changes
    <img src="/icon.svg" alt="Tlon logo" />
  </a>
  {
    links && (
      <nav>
        <ul class="flex">
          {links.map((link: any) => (
            <li>
              <a
                class:list={[
                  {
                    "dark:sm:hover:bg-mono-800 max-sm:hover:underline hover:sm:bg-navHover":
                      link.displayButton,
                  },
                  { "hover:underline": !link.displayButton },
                  "inline-block rounded-lg px-4 py-2",
                ]}
                href={link.url}
                target={link.blank ? "_blank" : "_self"}
              >
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </nav>
    )
  }
</header>
